/*
1. Create an array named fruits with the values 
['apple', 'banana', 'cherry', 'date', 'elderberry']. 
Use the slice method to create a new array named favoriteFruits 
that contains the first three fruits.
*/

/*
2. Create an array named numbers with the values [1, 2, 3, 4, 5, 6]. 
Use the slice method to create a new array named middleNumbers that 
contains the middle two numbers from the numbers array.
*/

/*
3. Create an array named colors with the values 
['red', 'blue', 'green', 'yellow']. Use the splice method to 
remove 'green' from the array.
*/

/*
4. Create an array named letters with the values ['a', 'b', 'd', 'e'].
Use the splice method to add 'c' at the correct position to fix the 
sequence.
*/

/*
5. Create an array named animals with the values [
'dog', 'cat', 'rabbit', 'mouse']. Use the splice method 
to replace 'rabbit' with 'hamster'
*/

/* 
1. Write a `for` loop that loops through the array `["green tea", "black tea", "chai", "oolong tea"]` 
and stops the loop when it finds `"chai"`. 
   Store all teas before `"chai"` in a new array named `selectedTeas`.
*/

/* 
2. Write a `for` loop that loops through the array `["London", "New York", "Paris", "Berlin"]` 
and skips `"Paris"`. 
   Store the other cities in a new array named `visitedCities`.
*/

/* 
3. Use a `for` loop to iterate through the array `[1, 2, 3, 4, 5]` and stop when the number `4` is found. 
   Store the numbers before `4` in an array named `smallNumbers`.
*/

/* 
4. Use a `for` loop to iterate through the array `["chai", "green tea", "herbal tea", "black tea"]` 
and skip `"herbal tea"`. 
   Store the other teas in an array named `preferredTeas`.
*/

/* 
5. Write a `for` loop that iterates through the array `[2, 5, 7, 9]`. 
   Skip the value `7` and multiply the rest by 2. Store the results in a new array named `doubledNumbers`.
*/


/* 
6. Write a `for` loop that calculates the sum of all numbers from 1 to 5 
and stores the result in a variable named `sum`.
*/

/* 
7. Write a `for` loop that counts down from 5 to 1 and stores the numbers in an array named `countdown`.
*/

/* 
8. Write a `for` loop that prompts a user to enter their favorite tea type until they enter `"stop"`. 
   Store each tea type in an array named `teaCollection`.
*/

/* 
9. Write a `for` loop that adds numbers from 1 to 3 and stores the result in a variable named `total`.
*/

/* 
10. Write a `for` loop that multiplies each element in the array `[2, 4, 6]` by 2 and stores the results in a new array named `multipliedNumbers`.
*/

/* 
11. Write a `for` loop that lists all the cities in the array `["Paris", "New York", "Tokyo", "London"]` and stores each city in a new array named `cityList`.
*/





// quiz
// Which of the following is the correct syntax for a for loop in JavaScript?
// for(let i = 0, i < 5, i++)
// for(i < 5; i++, i= 0)
// for(let i = 0; i < 5; i++)
// for i =0 to 5

// What will the following code output?

// for (let i = 0; i < 3; i++) {
//     console.log(i);
//    
// }
// 1 2 3
// 0 1 2 3
// 0 1
// 0 1 2
